spring:
  application:
    name: go-messaging-service
  config:
    import: optional:configserver:https://config-r3jv.onrender.com

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
